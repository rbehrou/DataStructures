# Question:

Factorial:

$$
F_n =
\left
\{\begin{matrix}
1, & n = 0, \\
n * F_{n-1}, & n > 0.
\end{matrix}
\right.
$$

Input: an integer ($n >= 0$)

Output: $F_{n}$

# to build

g++ -g main.cpp -o main.exe
